import React, { useState, useEffect, useMemo } from 'react'
import { Link } from 'react-router-dom'
import { ROUTES } from '@/lib/constants'
import { useAuthStore } from '@/stores/authStore'
import { useToastStore } from '@/stores/toastStore'
import { Spinner } from '@/components/ui'
import { cn, formatCurrency } from '@/lib/utils'
import { 
  ChevronRight, 
  Sparkles,
  FileText,
  FileSpreadsheet,
  Calendar,
  RefreshCw,
  Download,
  TrendingUp,
  TrendingDown,
  ArrowUpRight,
  ArrowDownRight,
  Activity,
  BarChart3,
  PieChart as PieChartIcon,
  CheckCircle2,
  AlertTriangle,
  Landmark,
  ListTodo,
  TrendingUp as ForecastIcon,
  History,
  DollarSign
} from 'lucide-react'
import { motion, AnimatePresence } from 'framer-motion'
import {
  AreaChart, Area, BarChart, Bar, PieChart, Pie, Cell,
  RadarChart, Radar, PolarGrid, PolarAngleAxis, PolarRadiusAxis,
  XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer, Legend,
  ComposedChart, Line
} from 'recharts'
import * as XLSX from 'xlsx'
import { generateFinancialReport, FinancialReportData } from '@/services/pharmacy/financialReportService'
import { generateFinancialReportPdf } from '@/services/pharmacy/financialReportPdfService'

// ─── Color Palette ────────────────────────────────────────────


















































































































































































































































































































































































































































































































































































    item_name: 'Examination 














































































































































































                      <YAxis tick={{ fontSize: 9, fill: '#94a3b8', fontWeight: 600 }} axisLine={false} tickLine={false} tickFormatter={(v) => `${(v/1000).toFixed(0)}k`} />
                      <Tooltip content={<CustomTooltip />} />
                      <Area type="monotone" dataKey="allocation" name="Allocated (RM)" stroke="#6366f1" fill="url(#areaAlloc)" strokeWidth={2} />
                      <Area type="monotone" dataKey="expenses" name="Spent (RM)" stroke="#10b981" fill="url(#areaExp)" strokeWidth={2} />
                    </AreaChart>
                  </ResponsiveContainer>
                </div>
              </motion.div>

              {/* Utilization distribution donut */}
              <motion.div
                initial={{ opacity: 0, y: 15 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: 0.2 }}
                className="bg-white/70 backdrop-blur-md rounded-3xl border border-slate-200/60 shadow-sm p-6 hover:shadow-xl hover:border-indigo-200/40 hover:-translate-y-0.5 transition-all duration-300 relative overflow-hidden"
              >
                <div className="flex items-center justify-between mb-4">
                  <div>
                    <h4 className="text-xs font-black text-slate-800 uppercase tracking-wider">Execution By Category</h4>
                    <p className="text-[10px] text-slate-400 font-bold">Utilization shares by item class</p>
                  </div>
                  <div className="w-8 h-8 rounded-xl bg-purple-50 flex items-center justify-center border border-purple-100">
                    <PieChartIcon className="w-4 h-4 text-purple-500" />

































































































































































































































































































































































































































































                    </div>

                    <div className="overflow-x-auto rounded-2xl border border-slate-200/80">
                      <table className="w-full text-left text-xs border-collapse">
                        <thead>
                          <tr className="bg-slate-50 border-b border-slate-200 text-slate-500 font-bold uppercase tracking-wider">
                            <th className="p-4">Item Name</th>
                            <th className="p-4">Item Code</th>
                            <th className="p-4">Category</th>
                            <th className="p-4">Department</th>
                            <th className="p-4">Vote Code</th>
                            <th className="p-4 text-right">Quantity Used</th>
                            <th className="p-4 text-right">Total Spent</th>
                          </tr>
                        </thead>
                        <tbody className="divide-y divide-slate-100">
                          {filteredItems.length === 0 ? (
                            <tr>
                              <td colSpan={7} className="p-8 text-center text-slate-400 font-semibold italic">
                                No items match your search.
                              </td>
                            </tr>
                          ) : (
                            filteredItems.map((
































































































































































































































































































































































































































































                            </div>

                          </div>
                        </div>
                      );
                    })()}
                  </div>
                )}

                {/* ════════ DETAILED TRANSACTIONS ════════ */}
                {activeTab === 'transactions' && (
                  <div className="p-6 sm:p-8 space-y-6">
                    <SectionHeader title="Ledger Transaction Auditing Logs" subtitle="Verify and audit every Purchase Order debited from hospital balances" icon={History} />
                    
                    {/* Auditing Inner Filters */}
                    <div className="flex flex-wrap items-center gap-3 bg-slate-100/80 backdrop-blur-md p-4 rounded-2xl border border-slate-200/50">
                      <div className="flex-1 min-w-[200px]">
                        <input
                          type="text"
                          placeholder="Search PO # or supplier..."
                          value={txnSearch}
                          onChange={e => { setTxnSearch(e.target.value); setTxnPage(1); }}
                          className="w-full text-xs font-semibold text-slate-700 bg-white border border-slate-200/60 rounded-xl p-2.5 outline-none focus:ring-2 focus:ring-indigo-500/20 focus:border-indigo-500 transition-all duration-200 shadow-inner"
                        />
                      </div>
                      <div className="min-w-[120px]">
                        <select
                          value={txnVoteFilter}
                          onChange={e => { setTxnVoteFilter(e.target.value); setTxnPage(1); }}
                          className="w-full text-xs font-bold text-slate-700 bg-white border border-slate-200/60 rounded-xl p-2.5 outline-none focus:ring-2 focus:ring-indigo-500/20 focus:border-indigo-500 transition-all duration-200"
                        >
                          <option value="all">All Vots</option>
                          <option value="080702">080702</option>
                          <option value="990102">990102</option>
                        </select>
                      </div>
                      <div className="min-w-[140px]">
                        <select
                          value={txnCategoryFilter}
                          onChange={e => { setTxnCategoryFilter(e.target.value); setTxnPage(1); }}
                          className="w-full text-xs font-bold text-slate-700 bg-white border border-slate-200/60 rounded-xl p-2.5 outline-none focus:ring-2 focus:ring-indigo-500/20 focus:border-indigo-500 transition-all duration-200"
                        >
                          <option value="all">All Categories</option>
                          {Array.from(new Set(reportData.transactions.map(t => t.category))).map((cat: any) => (
                            <option key={cat} value={cat}>{cat}</option>
                          ))}
                        </select>
                      </div>
                      <div className="min-w



















































































































































































































































                                {item.reason}
                              </p>
                            </div>
                          </div>

                        </div>
                      ))}
                    </div>

                  </div>

                  {/* Drawer Footer */}
                  <div className="px-6 py-4 border-t border-slate-800 bg-slate-950 flex justify-end space-x-3">
                    <button
                      onClick={() => setActiveDrawerId(null)}
                      className="bg-slate-800 text-slate-300 border border-slate-700 px-5 py-2.5 rounded-xl text-xs font-bold hover:bg-slate-700 transition-colors"
                    >
                      Dismiss Panel
                    </button>
                    {isDeficit && (
                      <button
                        onClick={() => {
                          setSandboxTarget(cat.id);
                          setSandboxAmount(shortfall);
                          setSandboxActive(true);
                          setActiveDrawerId(null);
                        }}
                        className="bg-indigo-650 text-white border border-indigo-600 px-5 py-2.5 rounded-xl text-xs font-bold hover:bg-indigo-700 transition-colors shadow"
                      >
                        Simulate Top-Up
                      </button>
                    )}
                  </div>

                </div>
              </div>
            </div>
          );
        })()}
      </div>
    </div>
  )
}
